
	<% @posts.each do |post| %>
		<h2> <%= post.title %> </h2>
		<p> <%= post.created_at.strftime('%A, %B, %d') %></p>
		<div><%= truncate(post.body, :length => 256) %></div>
		<br>
		<%= link_to "View full post.", post %>
	<% end %>

	<%= will_paginate @posts %>